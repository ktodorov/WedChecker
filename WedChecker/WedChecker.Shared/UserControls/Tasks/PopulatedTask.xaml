<UserControl
    x:Class="WedChecker.UserControls.Tasks.PopulatedTask"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WedChecker.UserControls.Tasks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Name="mainBorder" Style="{StaticResource DisplayBorderStyle}" Margin="0, 5, 0, 0">
        <Grid Name="mainGrid" Height="Auto" Background="#DCDCD8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition MaxWidth="50"/>
            </Grid.ColumnDefinitions>

            <Button Name="buttonTaskName" VerticalAlignment="Center" FontFamily="Segoe UI" 
                    FontSize="30" FontWeight="Light"
                HorizontalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                    BorderThickness="0" Click="buttonTaskName_Click">
                <TextBlock Text="Task Name" TextWrapping="WrapWholeWords"/>
            </Button>
            
            <Button Name="editTask" Content="&#xE104;" FontFamily="Segoe UI Symbol" FontSize="30"
                Height="50" Width="50" BorderThickness="0" Foreground="{StaticResource PhoneAccentBrush}"
                VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="1" Click="editTask_Click">
            </Button>

            <TextBlock Name="tbTaskHeader" Style="{StaticResource DisplayTextBlockStyle}" Grid.Row="1" Grid.ColumnSpan="2"
                       TextAlignment="Center" Margin="0"/>
            
            <Border Name="borderSplitter" Style="{StaticResource WedCheckerBorderSplitter}" Grid.Row="2" 
                    Grid.ColumnSpan="2" Margin="15, 5"/>
            
            <StackPanel Name="spConnectedControl" Grid.Row="3" Grid.ColumnSpan="2">
            </StackPanel>
            
            <Button Name="displayTask" Content="SUBMIT" FontFamily="Segoe UI" FontWeight="Light"
                    FontSize="30" Visibility="Collapsed" BorderThickness="0"
                    Foreground="{StaticResource PhoneAccentBrush}" Height="50"
                    VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="4" 
                    Grid.Column="0" Grid.ColumnSpan="2" Click="displayTask_Click">
            </Button>
        </Grid>
    </Border>
</UserControl>
