<UserControl
    x:Class="WedChecker.UserControls.TaskTileControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WedChecker.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Name="tileBorder" BorderThickness="1" BorderBrush="{ThemeResource SystemControlBackgroundBaseMediumLowBrush}">
        <Grid Name="tileGrid" Height="130" Width="180" 
          Background="{ThemeResource SystemControlBackgroundBaseHighBrush}">
            <TextBlock Name="tbTaskTitle" Text="{Binding TaskTitle}" VerticalAlignment="Center" 
                   HorizontalAlignment="Center" Margin="10" Padding="5"
                   FontSize="17" FontWeight="SemiLight" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                   TextWrapping="WrapWholeWords" TextAlignment="Center"/>

			<TextBlock Name="tbTaskName" Visibility="Collapsed" />
		</Grid>
    </Border>
</UserControl>
